%table.table.table-bordered
  %tr.table-primary
    %th Try
    - @try_things.each do |try_thing|
      %tr
        %td
          - if try_thing.complete?
            %span.badge.badge-success.mr-3
              Keep済み
          = try_thing.content
          %p.float-right
            = try_thing.created_at.strftime('%Y年%m月%d日')
