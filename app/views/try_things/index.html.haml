%table.table.table-bordered
  %tr.table-primary
    %th Try
    - @try_things.each do |try_thing|
      %tr
        %td
          = try_thing.content
          %p.float-right
            = try_thing.created_at.strftime('%Y年%m月%d日')