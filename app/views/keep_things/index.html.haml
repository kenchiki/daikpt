%table.table.table-bordered
  %tr.table-success
    %th Keep
    - @keep_things.each do |keep_thing|
      %tr
        %td
          - if keep_thing.complete?
            %span.badge.badge-info.mr-3
              習慣化
          - else
            %span.badge.badge-warning.mr-3
              実施中
          = keep_thing.content
          %p.d-inline.float-right
            = keep_thing.created_at.strftime('%Y年%m月%d日')


